# Nur fuer CLI/Tests der reinen Python-Module unter sunside/.
# Die Browser-App (index.html + Pyodide) braucht KEIN dieser Pakete auf dem
# Server: astral und gpxpy werden zur Laufzeit per micropip geladen.
astral>=3.2
gpxpy>=1.6
requests>=2.31
pytz>=2024.1

# Optional: GTFS-API-Server fuer den PWA-Fahrplan-Modus.
# Wenn du nur Streamlit oder die PWA ohne GTFS nutzt, kann das raus.
fastapi>=0.110
uvicorn[standard]>=0.29
